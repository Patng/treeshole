$boxes = $('<%= j render(@pins) %>')

$('#pins').append( $boxes );
<% if @pins.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate(@pins) %>');
<% else %>
  $('.pagination').remove();
<% end %>